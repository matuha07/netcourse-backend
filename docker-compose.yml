services:
  netcourse-db:
    image: postgres:18.3-alpine
    container_name: netcourse-db
    environment:
      - POSTGRES_USER=dummy
      - POSTGRES_PASSWORD=dummypass
      - POSTGRES_DB=netcourse
    volumes:
      - ./postgres-data:/var/lib/postgresql
    ports:
      - "5432:5432"
