version: "3.8"
services:
  postgres:
    image: postgres:10.3
    environment:
      - POSTGRES_USER=dummy
      - POSTGRES_PASSWORD=dummypass
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5433:5432"
volumes:
  postgres:
