services:
  postgres:
    image: postgres:10.3
    environment:
      - POSTGRES_USER=dummy
      - POSTGRES_PASSWORD=dummypass
      - POSTGRES_DB=netcourse
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgres:
